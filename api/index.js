import app from "../src/server.js";

/**
 * This module exports the Express application instance.
 *
 * By exporting the app, you allow it to be used in other parts of your application,
 * such as in serverless functions, testing frameworks, or other integration setups.
 */

export default app;
